#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define endl '\n'

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int tc; cin >> tc;
    while(tc--)
    {
    	ll N , K; cin >> N >> K;
    	vector<int>v(N);
    	for(int i = 0 ; i < N;i++) cin >> v[i];
    	vector<int>u(N);
        u[0] = 1;
    	for(int i = 0;i < N;i++)
    	{
    		


    	}	


    }
    
    return 0;
}