	#include<bits/stdc++.h>
	using namespace std;
	#define ll long long int
	#define endl '\n'

	int main(){
	    ios_base::sync_with_stdio(false);
	    cin.tie(NULL);
	    int n; cin >> n;
	    string s = "";
	    while(n--) {
	    	string str;
	    	getline(cin , str);
	    	cout << str;
	    }
	    
	    return 0;
	}